[[headers]]
  # Define which paths this specific [[headers]] block will cover.
  for = "/*"
  [headers.values]
    # Multi-value headers are expressed with multi-line strings.
    cache-control = "public, max-age=86400" # one day caching
